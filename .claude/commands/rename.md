---
description: Propose a better name for a device or entity, then rename it and patch all consumers once approved
---

Run the `ha-rename` skill against $ARGUMENTS — a device name/id or an `entity_id` (e.g. `light.lr_1`, or a device name like "Living Room Plug 3"). Follow the skill's steps exactly.
