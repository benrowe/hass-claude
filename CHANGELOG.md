# Changelog

Log of changes made to this Home Assistant instance.

## 2026-06-24

1. Rewired 5 holiday-mode automations (`Alert: Holiday Light On`, `Routine: Holiday Lights`, `Routine: Holiday Lights Off`, `Holiday: Front Door Opens - Lights On`, `Holiday: Door Opened Timer Finished - Lights Off`) to target individual lights instead of Hue Bridge groups; excluded `light.coffee_machine` and `light.kitchen_light_bar` from the kitchen/dining "lights on" triggers since those have dedicated automations.
2. Disabled all 16 known Hue group entities (`light.kitchen`, `light.living_room`, `light.living_room_2`, `light.dining_room`, `light.bathroom`, `light.hallway`, `light.man_cave`, `light.entry`, `light.study`, `light.toilet`, `light.garage`, `light.zone_hallway`, `light.zone_kitchen`, `light.zone_living_area`, `light.backyard`, `light.front_yard`).

## 2026-06-25

1. Fixed a regression from the 2026-06-24 Hue-group disable: that pass only re-audited the 5 holiday-mode automations, missing 4 others that also referenced the now-disabled groups. Swept all 67 automations directly via config and found: `Hallway: Motion` (`automation.hallway_sensor`) still targeted `light.hallway` — retargeted to `light.hallway_downlight_1`/`light.hallway_downlight_2`. Verified live: user confirmed hallway lights turn on with motion.
2. `Mode: Holiday Off` (`automation.mode_holiday_off`) still targeted `light.living_room` for the welcome-home lights — retargeted to `light.living_room_main`/`light.living_room_lamp`. Verified live: toggling `input_boolean.holiday_mode` on then off turned both lights on at brightness 76/255 (≈30%), matching `brightness_pct: 30`.
3. `Mode: Man Cave Off` (`automation.man_cave_toggle_off`) had conditions checking `light.backyard`/`light.kitchen` (disabled groups) before turning on backyard/kitchen lights post-dusk — replaced with state conditions against the real per-fixture entity lists (`light.backyard_floodlight`/`_pedestal_1`/`_pedestal_2`/`_spotlight_1`/`_spotlight_2`; `light.kitchen_downlight_1`/`_2`/`_3`/`_1_2`). Condition logic verified via template eval; full live test deferred to after dusk — logged in `VERIFY.md`.
4. `Presence: Arrival Lights` (`automation.presence_arrival_lights`) had conditions AND action target referencing `light.living_room`/`light.dining_room` (disabled groups) — replaced with `light.living_room_main`/`light.living_room_lamp`/`light.dining_room_main`. Untestable now (needs sunset + a real arrival/door-open event) — logged in `VERIFY.md`.
5. Worked `ISSUES.md` backlog via `/issues`: renamed `sensor.51244436_01ea_4e6b_afd3_049e3ce3efd0_3_illuminance` ("Kitchen Motion Sensor Illuminance", raw UUID entity_id) to `sensor.kitchen_illuminance` ("Kitchen Illuminance"). Searched automations/scripts/scenes/dashboards first — zero consumers found, nothing to patch. Verified live: new entity_id reports the same sensor value, old entity_id returns 404. While investigating, found `binary_sensor.sensor_1`'s device is misassigned to `area_id: entry` despite being named "Kitchen Motion Sensor" — logged as a new `ISSUES.md` entry rather than guessed at.
6. Worked `ISSUES.md` backlog via `/issues`: the 3 `Holiday:`-prefixed automations didn't fit any of the 7 documented categories, while 6 other holiday automations already used the `{Category}: Holiday {Trigger}` pattern. Renamed (alias + entity_id) into the Room category to match: `Holiday: Front Door Opens - Lights On` → `Front Door: Holiday Lights On` (`automation.front_door_holiday_lights_on`), `Holiday: Door Opened Timer Finished - Lights Off` → `Front Door: Holiday Lights Off` (`automation.front_door_holiday_lights_off`), `Holiday: Front Door Opened Timer` → `Front Door: Holiday Timer Started` (`automation.front_door_holiday_timer_started`). No consumers referenced the old entity_ids. Verified live: all 3 report the new names and state `on`.
7. Worked `ISSUES.md` backlog via `/issues`: renamed `Garage override: On`/`Off` aliases to `Garage: Override On`/`Off` to match sibling Garage automations (entity_ids unchanged — `Garage: Override Off`'s entity_id is called via `automation.trigger` from `Garage: Auto disable override when doors close`, so renaming it would've required patching that caller for no real benefit). While investigating, found the actual cause of a separately user-reported bug ("garage lights aren't turning on when the door opens"): `light.garage` — one of the 16 disabled Hue groups from the 2026-06-24 cleanup — was still referenced by 3 garage automations (`Garage: Open` condition+action, `Garage: Close` action, `Garage: Override Off` action), missed during manual review of that sweep's output even though all 3 were fetched in it. Retargeted all 3 to `light.garage_lights_main`. Verified live: closed then reopened `cover.garage_door_car` — `light.garage_lights_main` turned on the instant the door started opening.
8. Worked `ISSUES.md` backlog via `/issues`: resolved the Main TV ambiguity — `media_player.living_room` is a Bose Soundbar 700 (not a TV); the real Main TV is `media_player.living_room_tv_2_2` ("Living Room Sony TV", Sony BRAVIA 4K AE2), already used by `Living Room: TV On`/`Off`. Its `unavailable` state reflects the TV being powered off, not a broken entity. Assigned its device to the `living_room` area (was previously unassigned).
9. Worked `ISSUES.md` backlog via `/issues`: set MDI icons on the 8 automations that had none, per `CONVENTIONS.md`'s icon guidelines — `Alert: Meater Cook Started` (`mdi:pot-steam`), `Alert: Meater Estimated Time Available` (`mdi:clock-check-outline`), `Away Mode: AC Off Response` (`mdi:air-conditioner`), `Front Door: Holiday Lights On` (`mdi:home-lightbulb`), `Front Door: Holiday Lights Off` (`mdi:lightbulb-off`), `Front Door: Holiday Timer Started` (`mdi:timer-outline`), `Kitchen: Light Bar On With No Motion` (`mdi:lightbulb-off-outline`), `Kitchen: Light Bar Turned Off` (`mdi:lightbulb-off`).
10. Worked `ISSUES.md` backlog via `/issues`: `Away Mode: AC Off Response` used a prefix not in the 7 documented categories. It's the notification-action handler for `Presence: Everyone Left`'s away-mode prompt, so renamed (alias + entity_id) to `Presence: AC Off Response` (`automation.presence_ac_off_response`) to group it with its trigger source rather than inventing a new category. No consumers referenced the old entity_id. Verified live: reports the new name, state `on`. Moved its `STATE.md` listing from Mode to Presence (9 each, total still 67).
11. Worked `ISSUES.md` backlog via `/issues`: renamed `light.coffee_machine` → `light.dining_room_coffee_machine` and `light.dehumidifier` → `light.garage_dehumidifier` to match their device areas, fixing the `light.<area>_<fixture>` pattern violation. Patched all 8 consumer automations (`Alert: Coffee Machine Left On`, `Device: Coffee Machine Off via Notification`, `Routine: Morning`, `Dining Room: Coffee Machine On`, `Alert: Coffee Ready`, `Mode: Holiday On`, `Presence: Everyone Left`, `Routine: Bedtime`). Verified live: old entity_ids return 404, new ones report state correctly. While patching, noticed pre-existing template anti-patterns in two of those automations — logged as a new `ISSUES.md` entry rather than fixed in scope.
